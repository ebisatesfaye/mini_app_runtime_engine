## 1.0.0

* **Breaking Change**: Replaced all mocked fallback responses with strict native OS evaluations.
* Integrated `image_picker` for Camera queries.
* Integrated `geolocator` for exact physical coordinates requests.
* Integrated `shared_preferences` isolated by `appId` namespace for storage capability.
* Added native UX-focused `AlertDialog` wrapper for logical permission verification before granting Auth or Payment access.
* Expanded runtime hardware support for Bluetooth, Microphone, Contacts, Validators, and Sensors via `permission_handler`.

## 0.0.1

* Initial draft.
